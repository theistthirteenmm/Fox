# 🦊 راهنمای روباه CLI

رابط خط فرمان فارسی برای دستیار هوشمند روباه

## نصب و راه‌اندازی

### Windows
```bash
# اجرای مستقیم از ریشه پروژه
cli.bat

# یا از پوشه scripts
scripts\robah_cli.bat

# یا با Python
python robah_cli.py
```

### Linux/Mac
```bash
# اجرای مستقیم از ریشه پروژه
./cli.sh

# یا از پوشه scripts
./scripts/robah_cli.sh

# یا با Python
python3 robah_cli.py
```

### نصب کتابخانه‌ها
```bash
pip install -r requirements_cli.txt
```

## حالت‌های اجرا

### 1. حالت تعاملی (پیش‌فرض)
```bash
python robah_cli.py
```
- مکالمه مداوم با روباه
- دسترسی به تمام دستورات
- ذخیره تاریخچه مکالمه

### 2. حالت Backend
```bash
python robah_cli.py --backend
```
- اتصال به سرور FastAPI
- استفاده از تمام قابلیت‌های وب
- همگام‌سازی با رابط گرافیکی

### 3. پیام مستقیم
```bash
python robah_cli.py "سلام روباه چطوری؟"
```
- ارسال یک پیام و دریافت پاسخ
- مناسب برای اسکریپت‌نویسی
- خروج خودکار بعد از پاسخ

## دستورات CLI

### دستورات سیستم
| دستور فارسی | دستور انگلیسی | توضیح |
|-------------|----------------|-------|
| `/کمک` | `/help` | نمایش راهنما |
| `/وضعیت` | `/status` | وضعیت سیستم |
| `/پاک` | `/clear` | پاک کردن صفحه |
| `/خروج` | `/exit` | خروج از برنامه |

### مدیریت یادگیری
| دستور فارسی | دستور انگلیسی | توضیح |
|-------------|----------------|-------|
| `/یادگیری` | `/learn` | منوی یادگیری |
| `/نام` | `/name` | تغییر نام دستیار |
| `/صادرات` | `/export` | صادر کردن داده‌ها |

### اطلاعات
| دستور فارسی | دستور انگلیسی | توضیح |
|-------------|----------------|-------|
| `/تاریخچه` | `/history` | تاریخچه مکالمه |
| `/مدل‌ها` | `/models` | لیست مدل‌ها |

## ویژگی‌های یادگیری

### یادگیری واژگان
```
شما: وقتی میگم "انی" منظورم "این" هست
روباه: فهمیدم! از این به بعد وقتی 'انی' رو بگی، می‌دونم منظورت 'این' هست 📚
```

### یادگیری قوانین
```
شما: از این به بعد وقتی کد می‌خوام توضیحات فارسی هم اضافه کن
روباه: چشم! یاد گرفتم. از این به بعد همیشه توضیحات فارسی هم به کدها اضافه می‌کنم 📋
```

### تغییر لحن
```
شما: لحنت دوستانه‌تر باشه
روباه: باشه! از این به بعد لحنم دوستانه‌تر می‌شه 🎭
```

### تغییر نام
```
شما: اسمت نیلا باشه
روباه: عالی! از الان اسمم نیلا هست! ممنون که این اسم قشنگ رو برام انتخاب کردی 😊
```

## منوی یادگیری

دستور `/یادگیری` منوی کاملی ارائه می‌دهد:

```
🧠 منوی مدیریت یادگیری‌ها:

1. نمایش واژگان یادگیری شده
2. نمایش قوانین فعال  
3. نمایش ترجیحات لحن
4. اضافه کردن واژه جدید
5. اضافه کردن قانون جدید
6. نمایش خلاصه یادگیری
7. ریست تمام یادگیری‌ها
0. بازگشت به منوی اصلی
```

## مثال‌های کاربردی

### مکالمه عادی
```bash
$ python robah_cli.py

🦊 روباه CLI راه‌اندازی شد
✅ سیستم آماده است

شما: سلام روباه چطوری؟
روباه: سلام! خوشحالم که باهام حرف می‌زنی! حالم خوبه، تو چطوری؟ 🦊

شما: درباره برنامه‌نویسی Python بگو
روباه: Python زبان برنامه‌نویسی قدرتمند و ساده‌ایه که برای...
```

### استفاده در اسکریپت
```bash
# پرسش سریع
python robah_cli.py "آب و هوای امروز چطوره؟"

# در اسکریپت bash
RESPONSE=$(python robah_cli.py "تاریخ امروز چیه؟")
echo "پاسخ روباه: $RESPONSE"
```

### مدیریت یادگیری
```bash
شما: /یادگیری
# انتخاب گزینه 4 برای اضافه کردن واژه
واژه جدید: کیبورد
معنی: صفحه کلید
✅ واژه 'کیبورد' با معنی 'صفحه کلید' اضافه شد
```

## نکات مهم

### پشتیبانی از فارسی
- CLI کاملاً از فارسی پشتیبانی می‌کند
- تمام منوها و پیام‌ها به فارسی هستند
- امکان استفاده از دستورات فارسی و انگلیسی

### عملکرد
- حالت مستقل: بدون نیاز به backend
- حالت Backend: اتصال به سرور FastAPI
- تشخیص خودکار دسترسی به backend

### ذخیره‌سازی
- تاریخچه مکالمه در حافظه
- یادگیری‌ها در فایل‌های JSON
- امکان صادرات و وارد کردن داده‌ها

### سازگاری
- Windows (cmd, PowerShell)
- Linux (bash, zsh)
- macOS (Terminal)
- Python 3.6+

## عیب‌یابی

### مشکلات رایج

**Python پیدا نمی‌شود:**
```bash
# Windows
where python
# Linux/Mac  
which python3
```

**کتابخانه‌ها نصب نیستند:**
```bash
pip install colorama requests
```

**Backend در دسترس نیست:**
- CLI خودکار به حالت مستقل می‌رود
- برای استفاده از backend، ابتدا سرور را راه‌اندازی کنید

**مشکل encoding فارسی:**
```bash
# Windows
chcp 65001

# Linux/Mac
export LANG=fa_IR.UTF-8
```

## مثال کامل

```bash
$ python robah_cli.py --backend

🦊 روباه CLI راه‌اندازی شد
✅ اتصال به backend برقرار شد

╔══════════════════════════════════════════════════════════════╗
║                    🦊 روباه - دستیار هوشمند                     ║
║                      رابط خط فرمان فارسی                        ║
╚══════════════════════════════════════════════════════════════╝

دستورات مفید:
• /help یا /کمک    - نمایش راهنما
• /status یا /وضعیت - وضعیت سیستم  
• /learn یا /یادگیری - مدیریت یادگیری‌ها

برای شروع، سؤال یا پیامت رو بنویس! 💬

شما: سلام روباه! اسمت رو عوض کنم؟
🤔 در حال فکر کردن...

روباه: سلام! البته، می‌تونی اسمم رو عوض کنی. چه اسمی دوست داری برام انتخاب کنی؟ 😊

شما: اسمت آریا باشه
🤔 در حال فکر کردن...

آریا: عالی! از الان اسمم آریا هست! ممنون که این اسم قشنگ رو برام انتخاب کردی 😊

شما: /وضعیت

📊 وضعیت سیستم روباه:

🔗 حالت: Backend (FastAPI)
🧠 مدل بارگذاری شده: ✅
💾 تعداد خاطرات: 15
👤 سطح شخصیت: 2.1
🌐 جستجوی وب: ✅
⏰ آخرین به‌روزرسانی: 2026-02-03T11:30:45

شما: /خروج
👋 خداحافظ! امیدوارم کمکت کرده باشم
```

---

**روباه CLI - دستیار هوشمند در دستان شما! 🦊✨**