# 💻 دستیار برنامه‌نویسی روباه

## 🌟 معرفی

روباه حالا یک دستیار برنامه‌نویسی قدرتمند است که می‌تواند:
- کد شما را تحلیل کند
- مشکلات را پیدا کند
- راه‌حل‌های بهتر پیشنهاد دهد
- کد را اصلاح کند
- بهترین practices را آموزش دهد

## ✨ قابلیت‌های برنامه‌نویسی

### 🔍 تحلیل کد
- **تشخیص زبان**: خودکار تشخیص زبان برنامه‌نویسی
- **بررسی Syntax**: کنترل صحت نحوی کد
- **پیدا کردن خطاها**: شناسایی مشکلات رایج
- **محاسبه پیچیدگی**: ارزیابی سطح پیچیدگی کد

### 🛠️ اصلاح خودکار
- **اصلاح Indentation**: تنظیم فاصله‌گذاری
- **بهبود نام‌گذاری**: پیشنهاد نام‌های بهتر
- **حذف کدهای اضافی**: پاک کردن بخش‌های غیرضروری
- **بهینه‌سازی**: بهبود عملکرد کد

### 📚 آموزش و راهنمایی
- **توضیح کد**: شرح خط به خط عملکرد
- **Best Practices**: آموزش بهترین روش‌ها
- **پیشنهاد بهبود**: راه‌های بهتر کدنویسی
- **جلوگیری از خطا**: نکات امنیتی و کیفی

## 🎯 زبان‌های پشتیبانی شده

### کاملاً پشتیبانی شده:
- **Python** (.py)
- **JavaScript** (.js, .jsx, .ts, .tsx)

### پشتیبانی پایه:
- **Java** (.java)
- **C++** (.cpp, .cc, .cxx)
- **C** (.c)
- **HTML** (.html, .htm)
- **CSS** (.css)
- **SQL** (.sql)
- **JSON** (.json)
- **XML** (.xml)

## 🚀 نحوه استفاده

### 1. ارسال کد در چت
فقط کد خود را در پیام بفرستید:

```python
def hello_world():
    print("Hello, World!")
```

### 2. استفاده از Code Blocks
```python
def calculate_sum(a, b):
    return a + b

result = calculate_sum(5, 3)
print(result)
```

### 3. درخواست مستقیم
- "این کد رو بررسی کن"
- "مشکل این کد چیه؟"
- "این کد رو بهینه کن"
- "این کد چطور کار می‌کنه؟"

## 📊 API Endpoints

### تحلیل کد
```http
POST /code/analyze
Content-Type: application/x-www-form-urlencoded

code=def hello(): print("hi")
language=python
```

### اصلاح کد
```http
POST /code/fix
Content-Type: application/x-www-form-urlencoded

code=def hello(): print("hi")
language=python
```

### زبان‌های پشتیبانی شده
```http
GET /code/languages
```

## 🔍 نمونه تحلیل

### کد ورودی:
```python
def calc(x,y):
print(x+y)

calc(5,3)
```

### نتیجه تحلیل:
- **مشکلات**:
  - خط 2: IndentationError - فاصله‌گذاری اشتباه
  - خط 1: بدون return value
  
- **پیشنهادات**:
  - اضافه کردن docstring
  - استفاده از type hints
  - return کردن نتیجه

### کد اصلاح شده:
```python
def calc(x: int, y: int) -> int:
    """محاسبه مجموع دو عدد"""
    return x + y

result = calc(5, 3)
print(result)
```

## 💡 نکات مهم

### برای Python:
- از PEP 8 پیروی کنید
- از type hints استفاده کنید
- docstring بنویسید
- از f-strings استفاده کنید

### برای JavaScript:
- از let/const به جای var استفاده کنید
- از === به جای == استفاده کنید
- semicolon فراموش نکنید
- error handling اضافه کنید

### برای همه زبان‌ها:
- نام‌گذاری معنادار
- کامنت‌گذاری مناسب
- کد تمیز و خوانا
- تست نوشتن

## 🎨 مثال‌های کاربردی

### 1. Debug کردن خطا
**کاربر**: "این کد خطا می‌ده، چرا؟"
```python
def divide(a, b):
    return a / b

print(divide(10, 0))
```

**روباه**: "مشکل در تقسیم بر صفر است! باید چک کنی که b صفر نباشه..."

### 2. بهینه‌سازی کد
**کاربر**: "این کد رو بهینه کن"
```python
numbers = [1, 2, 3, 4, 5]
squares = []
for num in numbers:
    squares.append(num * num)
```

**روباه**: "می‌تونی از list comprehension استفاده کنی..."

### 3. توضیح کد
**کاربر**: "این کد چطور کار می‌کنه؟"
```python
lambda x: x**2
```

**روباه**: "این یک lambda function است که..."

## 🔧 تنظیمات پیشرفته

### فعال‌سازی تحلیل خودکار:
کد را در پیام بفرستید و روباه خودکار تشخیص می‌دهد.

### سطح جزئیات:
- **ساده**: فقط مشکلات اصلی
- **متوسط**: مشکلات + پیشنهادات
- **کامل**: تحلیل عمیق + بهینه‌سازی

## 📈 آمار و گزارش

روباه آمار کدهای تحلیل شده را نگه می‌دارد:
- تعداد کدهای بررسی شده
- زبان‌های استفاده شده
- نوع مشکلات یافت شده
- میزان بهبود کد

---

🦊 **با روباه برنامه‌نویسی بهتری باش!**