# 🦊 راهنمای استقرار روباه در محیط جدید

## 📋 پیش‌نیازها

### 1. نرم‌افزارهای مورد نیاز:
- **Python 3.8+** - [دانلود](https://python.org)
- **Node.js 16+** - [دانلود](https://nodejs.org)
- **Ollama** - [دانلود](https://ollama.ai)
- **Git** - [دانلود](https://git-scm.com)

### 2. مدل AI (حجیم):
- **مدل فارسی**: `partai/dorna-llama3:8b-instruct-q8_0` (حدود 8.5 GB)

## 🚀 مراحل نصب

### مرحله 1: دانلود پروژه
```bash
git clone https://github.com/theistthirteenmm/Fox_v3.git
cd Fox_v3
```

### مرحله 2: نصب Ollama و مدل
```bash
# نصب Ollama (اگر نصب نیست)
# Windows: از ollama.ai دانلود کنید
# Linux/Mac: curl -fsSL https://ollama.ai/install.sh | sh

# راه‌اندازی Ollama
ollama serve

# دانلود مدل فارسی (در terminal جدید)
ollama pull partai/dorna-llama3:8b-instruct-q8_0
```

### مرحله 3: راه‌اندازی خودکار
**Windows:**
```cmd
scripts\start_robah.bat
```

**Linux/Mac:**
```bash
chmod +x scripts/*.sh
./scripts/start_robah.sh
```

## 📁 ساختار مهم پروژه

```
Fox_v3/
├── 🐍 backend/           # Python FastAPI Backend
├── ⚛️ frontend/          # React TypeScript Frontend  
├── 🧠 brain/             # AI Core & Logic
├── ⚙️ config/            # تنظیمات
├── 📊 data/              # داده‌ها و حافظه
├── 📚 docs/              # مستندات
├── 🔧 scripts/           # اسکریپت‌های اجرا
└── 📋 requirements.txt   # Python Dependencies
```

## 🌐 آدرس‌های دسترسی

بعد از راه‌اندازی موفق:
- **رابط اصلی**: http://localhost:3000
- **API Backend**: http://localhost:8000
- **مستندات API**: http://localhost:8000/docs
- **وضعیت سیستم**: http://localhost:8000/status

## ✨ ویژگی‌های کلیدی

### 🧠 هوش مصنوعی
- مدل فارسی LLaMA3 8B
- پاسخ‌های هوشمند و متنوع
- یادگیری از تعاملات

### 🎙️ سیستم صوتی
- تبدیل گفتار به متن (STT)
- تبدیل متن به گفتار (TTS)
- پخش خودکار پاسخ‌ها

### 💾 حافظه و یادگیری
- حافظه کوتاه‌مدت و بلندمدت
- Vector Database (ChromaDB)
- یادگیری از الگوهای مکالمه

### 🌐 جستجوی اینترنت
- Wikipedia API
- DuckDuckGo Search
- تلفیق اطلاعات آنلاین

### 📁 مدیریت فایل
- آپلود PDF, DOCX, تصاویر
- تحلیل محتوای فایل‌ها
- OCR برای تصاویر (آینده)

## 🔧 عیب‌یابی

### مشکلات رایج:

#### 1. خطای "Python not found"
```bash
# بررسی نصب Python
python --version
# یا
python3 --version
```

#### 2. خطای "Ollama connection failed"
```bash
# بررسی وضعیت Ollama
curl http://localhost:11434/api/tags

# راه‌اندازی مجدد
ollama serve
```

#### 3. خطای "Port already in use"
```bash
# توقف سرویس‌ها
scripts/stop_robah.bat    # Windows
./scripts/stop_robah.sh   # Linux/Mac
```

#### 4. خطای "Node modules not found"
```bash
cd frontend
npm install
```

### بررسی لاگ‌ها:
```bash
# مشاهده لاگ‌های سیستم
tail -f logs/robah.log     # Linux/Mac
type logs\robah.log        # Windows
```

## 📦 انتقال به سیستم جدید

### فایل‌هایی که باید کپی کنید:
1. **کل پروژه** (از Git)
2. **مدل Ollama** (اختیاری - برای سرعت):
   - Windows: `C:\Users\[USER]\.ollama\models\`
   - Linux/Mac: `~/.ollama/models/`

### مراحل انتقال:
1. Clone کردن repository
2. اجرای `scripts/start_robah.bat`
3. اگر مدل کپی نکردید، خودکار دانلود می‌شود

## 🔒 امنیت

- تمام ارتباطات محلی (localhost)
- عدم ارسال داده به سرورهای خارجی
- مدل AI محلی (Ollama)
- داده‌های شخصی محفوظ

## 📞 پشتیبانی

در صورت بروز مشکل:
1. بررسی فایل `logs/robah.log`
2. اجرای `scripts/stop_robah.bat` و سپس `scripts/start_robah.bat`
3. بررسی مستندات در پوشه `docs/`

---

🦊 **موفق باشید با روباه!**

تاریخ آخرین به‌روزرسانی: 2026-01-20
نسخه: 3.0.0