# 🦊 راهنمای سریع روباه

## مراحل راه‌اندازی (5 دقیقه)

### 1. نصب Ollama
```cmd
# برو به https://ollama.ai و Windows installer رو دانلود کن
# بعد از نصب:
ollama serve
```

### 2. دانلود مدل فارسی
```cmd
# مدل فارسی پیشنهادی (8.5GB):
ollama pull partai/dorna-llama3:8b-instruct-q8_0

# یا مدل‌های دیگر:
ollama pull llama3.2:3b        # سبک (2GB)
ollama pull codellama:13b      # کد (7GB)
```

### 3. راه‌اندازی روباه
```cmd
# روش ساده (پیشنهادی):
start_robah.bat

# یا دستی:
# Terminal 1: python backend/main.py
# Terminal 2: cd frontend && npm start
```

### 4. دسترسی
- **رابط وب**: http://localhost:3000
- **API**: http://localhost:8000
- **مستندات**: http://localhost:8000/docs

## ویژگی‌های فعال ✅

🎯 **هسته AI**: سیستم چند مدله هوشمند  
🧠 **حافظه**: ذخیره و بازیابی مکالمات  
🎭 **شخصیت**: رشد و تطبیق با کاربر  
🎤 **صوت**: ضبط و تبدیل به متن  
📁 **فایل**: آپلود و پردازش فایل‌ها  
🌐 **وب**: جستجوی اینترنت  
💻 **کد**: تحلیل برنامه‌نویسی  
💭 **Thinking**: پیام‌های میانی در پردازش  

## مدل‌های پشتیبانی شده

### سیستم چند مدله
- **فارسی**: `partai/dorna-llama3:8b-instruct-q8_0` (8.5GB)
- **عمومی**: `llama4:scout` (50GB) - قدرتمند
- **کد**: `codellama:13b` (7GB) - برنامه‌نویسی
- **سریع**: `llama4:scout-q4` (10GB) - پاسخ سریع

### دانلود همه مدل‌ها
```cmd
# دانلود خودکار همه مدل‌ها (67GB):
download_models.bat
```

## عیب‌یابی سریع

### بررسی سیستم
```cmd
# بررسی Ollama
curl http://localhost:11434/api/tags

# بررسی Backend  
curl http://localhost:8000/status

# لیست مدل‌ها
ollama list
```

### مشکلات رایج

**Frontend نمی‌آد بالا:**
```cmd
cd frontend
npm install
npm start
```

**Backend خطا می‌ده:**
```cmd
pip install -r requirements.txt
python backend/main.py
```

**میکروفون کار نمی‌کنه:**
- برو به: http://localhost:8000/speech/debug
- Console مرورگر رو باز کن (F12)
- دکمه میکروفون رو تست کن

## نیازمندی‌های سیستم

- **RAM**: حداقل 8GB (16GB بهتر)
- **فضای دیسک**: 10-70GB (بسته به مدل‌ها)
- **CPU**: هر پردازنده مدرن
- **OS**: Windows 10/11

## ویژگی‌های روباه

✅ **حافظه هوشمند**: همه مکالمات رو به خاطر می‌سپاره  
✅ **رشد شخصیت**: با هر مکالمه قوی‌تر می‌شه  
✅ **پشتیبانی فارسی**: کاملاً فارسی صحبت می‌کنه  
✅ **یادگیری مداوم**: از تعاملات یاد می‌گیره  
✅ **رابط زیبا**: طراحی مدرن و کاربرپسند  
✅ **سیستم چند مدله**: انتخاب هوشمند مدل بر اساس نیاز

---

🎉 **روباه آماده است تا با شما رشد کند!**

📚 **راهنماهای بیشتر:**
- `INSTALL.md` - نصب کامل
- `MODELS_DOWNLOAD_GUIDE.md` - راهنمای مدل‌ها  
- `docs/` - مستندات فنی