# 🦊 فلوی کامل عملکرد روباه

## 📥 **مرحله 1: دریافت پیام**
```
Frontend (React) → WebSocket → Backend (FastAPI) → process_user_message()
```
- کاربر پیام رو در frontend تایپ می‌کنه
- از طریق WebSocket به backend ارسال می‌شه
- `process_user_message()` در `backend/main.py` فراخوانی می‌شه

## 🧠 **مرحله 2: پردازش اولیه**
```
Memory Manager → Personality Engine → AI Brain
```
- **Memory Manager**: پیام رو در حافظه ذخیره می‌کنه
- **Personality Engine**: احساسات و شخصیت کاربر رو تحلیل می‌کنه  
- **AI Brain**: `generate_response()` شروع می‌شه

## 🔍 **مرحله 3: تحلیل پیام (در AI Brain)**
```
Topic Detection → Context Analysis → Code Detection → User Profiling
```
- **تشخیص موضوع**: آیا ادامه مکالمه قبلی هست یا موضوع جدید؟
- **تحلیل Context**: بررسی تاریخچه مکالمه
- **تشخیص کد**: آیا پیام شامل کد برنامه‌نویسی هست؟
- **پروفایل کاربر**: تحلیل الگوهای رفتاری کاربر

## 🌐 **مرحله 4: جستجوی وب (اختیاری)**
```
Web Search Engine → DuckDuckGo + Wikipedia
```
- اگر پیام نیاز به اطلاعات جدید داشته باشه
- جستجو در اینترنت و خلاصه‌سازی نتایج

## 🤖 **مرحله 5: انتخاب مدل و تولید پاسخ**
```
Model Selection → Ollama API → Persian LLM
```
- **انتخاب مدل**: فعلاً همیشه مدل فارسی (`partai/dorna-llama3:8b-instruct-q8_0`)
- **ساخت Prompt**: ترکیب پیام + context + اطلاعات وب
- **فراخوانی Ollama**: ارسال به `http://localhost:11434/api/generate`
- **دریافت پاسخ**: پاسخ خام از مدل AI

## 📚 **مرحله 6: بهبود پاسخ**
```
Dataset Enhancement → Response Structuring → Learning Prompt Creation
```
- **بهبود با Dataset**: استفاده از الگوهای یادگیری شده
- **ساختاردهی**: فرمت کردن پاسخ (اگر کد داشته باشه، اطلاعات وب، و...)
- **ایجاد Learning Prompt**: تبدیل مکالمه به prompt برای یادگیری آینده

## 💾 **مرحله 7: ذخیره‌سازی**
```
Memory Storage → Learning Data → User Profile Update
```
- **حافظه**: ذخیره پیام و پاسخ در ChromaDB
- **یادگیری**: ذخیره در `data/learning/conversations.jsonl`
- **پروفایل**: به‌روزرسانی پروفایل کاربر

## 📤 **مرحله 8: ارسال پاسخ**
```
AI Brain → Backend → WebSocket → Frontend
```
- پاسخ نهایی به backend برمی‌گرده
- از طریق WebSocket به frontend ارسال می‌شه
- کاربر پاسخ رو می‌بینه

## 🎙️ **مرحله 9: تولید صدا (اختیاری)**
```
TTS Request → Speech Handler → pyttsx3 → Audio File
```
- اگر کاربر روی دکمه صدا بزنه
- متن به صدا تبدیل می‌شه با `pyttsx3`
- فایل صوتی برای دانلود آماده می‌شه

---

## ⏱️ **زمان‌بندی معمولی:**
- **پیام‌های ساده**: 2-5 ثانیه
- **پیام‌های پیچیده**: 5-15 ثانیه  
- **پیام‌های با جستجوی وب**: 10-20 ثانیه

## 🔧 **اجزای کلیدی:**
- **Frontend**: React + TypeScript + WebSocket
- **Backend**: FastAPI + Python
- **AI Model**: Ollama + Persian LLM
- **Memory**: ChromaDB
- **Speech**: Whisper + pyttsx3
- **Web Search**: DuckDuckGo + Wikipedia

## 📊 **وضعیت فعلی سیستم:**
✅ **کاملاً فعال:**
- WebSocket چت
- مدل AI فارسی
- سیستم حافظه
- تشخیص موضوع
- جستجوی وب
- TTS (تبدیل متن به صدا)

⚠️ **نیاز به بهبود:**
- STT (تبدیل صدا به متن) - نیاز به PyAudio
- مدل‌های اضافی (کد، سریع، عمومی)
- سیستم‌های بهینه‌سازی پیشرفته

---

**تاریخ به‌روزرسانی**: 23 ژانویه 2026  
**نسخه**: 1.0 - سیستم کامل و آماده استفاده