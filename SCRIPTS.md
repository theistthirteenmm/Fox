# 🦊 اسکریپت‌های روباه

این فایل راهنمای استفاده از اسکریپت‌های راه‌اندازی روباه است.

## 📁 فایل‌های موجود در `scripts/`

### اسکریپت‌های اصلی (پیشنهادی)

#### Windows:
- **`scripts/start_robah.bat`** - اسکریپت کامل راه‌اندازی با بررسی‌های کامل
- **`scripts/run.bat`** - اسکریپت ساده و سریع
- **`scripts/stop_robah.bat`** - توقف تمام سرویس‌ها

#### Linux/Mac:
- **`scripts/start_robah.sh`** - اسکریپت کامل راه‌اندازی با بررسی‌های کامل
- **`scripts/run.sh`** - اسکریپت ساده و سریع  
- **`scripts/stop_robah.sh`** - توقف تمام سرویس‌ها

## 🚀 نحوه استفاده

### راه‌اندازی کامل (پیشنهادی)

**Windows:**
```cmd
scripts\start_robah.bat
```

**Linux/Mac:**
```bash
./scripts/start_robah.sh
```

### راه‌اندازی سریع

**Windows:**
```cmd
scripts\run.bat
```

**Linux/Mac:**
```bash
./scripts/run.sh
```

### توقف سرویس‌ها

**Windows:**
```cmd
scripts\stop_robah.bat
```

**Linux/Mac:**
```bash
./scripts/stop_robah.sh
```

## ✨ ویژگی‌های اسکریپت کامل

### بررسی‌های خودکار:
- ✅ بررسی نصب Python 3.8+
- ✅ بررسی نصب Node.js 16+
- ✅ بررسی وجود فایل‌های پروژه
- ✅ بررسی وضعیت Ollama
- ✅ بررسی وجود مدل AI

### راه‌اندازی خودکار:
- 🧠 راه‌اندازی Ollama Server
- 📥 دانلود مدل فارسی (در صورت نیاز)
- 🐍 ایجاد و فعال‌سازی Virtual Environment
- 📦 نصب Python Dependencies
- ⚛️ نصب Node.js Dependencies
- 📁 ایجاد دایرکتوری‌های مورد نیاز

### راه‌اندازی سرویس‌ها:
- 🔧 Backend (Python FastAPI)
- 🌐 Frontend (React)
- 🌍 باز کردن خودکار مرورگر

## 🔧 تنظیمات

### متغیرهای محیطی قابل تنظیم:

```bash
# مدل AI (اختیاری)
export ROBAH_MODEL="partai/dorna-llama3:8b-instruct-q8_0"

# آدرس Ollama (اختیاری)  
export OLLAMA_URL="http://localhost:11434"

# پورت Backend (اختیاری)
export ROBAH_PORT=8000

# پورت Frontend (اختیاری)
export PORT=3000
```

## 🌐 آدرس‌های دسترسی

بعد از راه‌اندازی موفق:

- **رابط وب**: http://localhost:3000
- **API Backend**: http://localhost:8000  
- **مستندات API**: http://localhost:8000/docs
- **وضعیت سیستم**: http://localhost:8000/status

## 🐛 عیب‌یابی

### مشکلات رایج:

#### 1. خطای "Python not found"
```bash
# نصب Python
# Windows: از python.org دانلود کنید
# Ubuntu/Debian: sudo apt install python3 python3-pip
# macOS: brew install python3
```

#### 2. خطای "Node.js not found"  
```bash
# نصب Node.js
# Windows: از nodejs.org دانلود کنید
# Ubuntu/Debian: sudo apt install nodejs npm
# macOS: brew install node
```

#### 3. خطای "Ollama not found"
```bash
# نصب Ollama
# Windows: از ollama.ai دانلود کنید
# Linux/Mac: curl -fsSL https://ollama.ai/install.sh | sh
```

#### 4. خطای "Port already in use"
```bash
# توقف سرویس‌ها
./stop_robah.sh  # Linux/Mac
stop_robah.bat   # Windows
```

#### 5. خطای "Permission denied" (Linux/Mac)
```bash
# اجازه اجرا دادن به اسکریپت‌ها
chmod +x *.sh
```

### بررسی لاگ‌ها:
```bash
# مشاهده لاگ‌های سیستم
tail -f logs/robah.log

# Windows
type logs\robah.log
```

## 📋 چک‌لیست قبل از اجرا

- [ ] Python 3.8+ نصب شده
- [ ] Node.js 16+ نصب شده  
- [ ] Ollama نصب شده
- [ ] فایل‌های پروژه موجود است
- [ ] پورت‌های 3000 و 8000 آزاد هستند

## 💡 نکات مهم

1. **اولین بار**: از اسکریپت کامل (`start_robah`) استفاده کنید
2. **اجراهای بعدی**: می‌توانید از اسکریپت سریع (`run`) استفاده کنید
3. **توقف**: همیشه از اسکریپت توقف استفاده کنید تا فرآیندها صحیح متوقف شوند
4. **مشکل**: در صورت بروز مشکل، ابتدا سرویس‌ها را متوقف کنید سپس دوباره راه‌اندازی کنید

## 🎯 مثال کامل استفاده

```bash
# راه‌اندازی اولیه
./start_robah.sh

# استفاده از روباه...

# توقف سرویس‌ها
./stop_robah.sh

# راه‌اندازی مجدد (سریع)
./run.sh
```

---

🦊 **لذت ببرید از استفاده از روباه!**