# کتابخانه‌های مورد نیاز برای روباه CLI
colorama>=0.4.4      # رنگ‌های ترمینال
requests>=2.25.1     # ارتباط با backend
readline             # تاریخچه و auto-complete (معمولاً پیش‌فرض نصب است)
numpy>=1.21.0        # پردازش صوت
sounddevice>=0.4.6   # ضبط و پخش صوت
soundfile>=0.12.1    # خواندن/نوشتن فایل صوتی
